#!/bin/sh

pgrep -x transmission-da || (transmission-daemon && pgrep -x dunst >/dev/null && (notify-send -a "Transmission Daemon" "Starting transmission-daemon...") ; sleep 3s)
transmission-remote -a "$@" && (pgrep -x dunst >/dev/null && notify-send -a "Transmission Daemon" "Added torrent" "$@ has been added to queue")
