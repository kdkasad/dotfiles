#!/bin/sh

if [ "$1" = "-g" ]; then
	shift
	CHOOSER="bemenu $@"
	VIEWER="$TERMINAL -e man"
else
	CHOOSER="fzf +x --reverse --height=60% $@"
	VIEWER="man"
fi

apropos . | $CHOOSER | awk '{print $2 $1}' | tr -d '(' | tr ')' ' ' |
	xargs -r $VIEWER
