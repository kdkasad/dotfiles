#!/bin/sh

if command -v pamixer >/dev/null 2>&1; then # PulseAudio
	# Un-mute and increase volume
	pamixer -u -i 5
else # ALSA
	# Un-mute
	amixer sset Master on
	# Increase volume
	amixer sset Master 5%+
fi

# Send volume notification
notifyvolume
