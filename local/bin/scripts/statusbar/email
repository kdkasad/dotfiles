#!/bin/sh

case $BUTTON in
	1) setsid -f $TERMINAL -e mutt ;;
	3) setsid -f mailsync ;;
esac >/dev/null 2>&1

find ${MAILDIR:-/var/spool/mail/$(id -un)}/*/INBOX/new -type f | wc -l | sed 's/^0$//; /^$/q;'
