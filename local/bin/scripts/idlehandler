#!/bin/sh

# Start screen locker
slock &

# Don't sleep if $HOME/.local/share/nosleep exists
if [ -e $HOME/.local/share/nosleep ]; then
	printf '%s exists. Not sleeping.\n' $HOME/.local/share/nosleep >&2
	exit 0
fi

systemctl suspend
